// AnguLogin Stealth Extension - Service Worker
// Minimal background script required by MV3. Placeholder for future features.
chrome.runtime.onInstalled.addListener(() => {
    // Extension loaded successfully
});
