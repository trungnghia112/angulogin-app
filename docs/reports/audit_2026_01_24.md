# Audit Report - 2026-01-24

## Summary
- 🔴 Critical Issues: 3
- 🟡 Warnings: 1
- 🟢 Suggestions: 2

## 🔴 Critical Issues (Phải sửa ngay)
1. **Lộ File Bí Mật (.env)**
   - **File:** `.gitignore` (Thiếu khai báo `.env`)
   - **Nguy hiểm:** File `.env` chứa `STITCH_ACCESS_TOKEN` hiện đang bị Git theo dõi. Nếu anh push lên GitHub, hacker có thể chiếm quyền điều khiển tài khoản Stitch của anh ngay lập tức.
   - **Cách sửa:** Thêm `.env` vào file `.gitignore` và dùng lệnh `git rm --cached .env` để gỡ nó khỏi bộ nhớ Git.

2. **Cửa Nhà Đang Mở (Firestore Rules)**
   - **File:** `firestore.rules`
   - **Nguy hiểm:** Rule hiện tại cho phép **BẤT KỲ AI** cũng có thể đọc, sửa, xóa toàn bộ dữ liệu database của anh (Test Mode). Hacker có thể phá sạch dữ liệu chỉ trong vài giây.
   - **Cách sửa:** Cần viết lại rules để giới hạn quyền truy cập theo User ID (`request.auth.uid`).

3. **Lỗi Cấu Pháp CSS (Build Warning)**
   - **File:** `src/styles.css` (hoặc các file được import)
   - **Nguy hiểm:** Build lỗi `Expected identifier but found "-"` tại dòng 2205 (vùng code sinh ra từ Tailwind/PrimeUI). Điều này có thể làm vỡ giao diện hoặc mất các style quan trọng.
   - **Cách sửa:** Kiểm tra lại các biến CSS tùy chỉnh hoặc các class Tailwind xem có dấu `-` bị đặt sai chỗ không.

## 🟡 Warnings (Nên sửa)
1. **Hardcoded API Keys**
   - **File:** `src/environments/environment.ts` & `src/environments/environment.prod.ts`
   - **Vấn đề:** API Key của Firebase đang để trực tiếp trong code. Dù Firebase key mang tính công khai, nhưng để trong environment variables sẽ an toàn hơn khi làm việc nhóm.

## 🟢 Suggestions (Tùy chọn)
1. **Dọn dẹp Log:** Xóa bớt các TODO hoặc console.log rải rác nếu không còn dùng đến.
2. **Persistence:** Tính năng Dark Mode anh vừa làm cần lưu lại vào `localStorage` để khi F5 không bị mất (Em đã nhắc anh ở bước trước).

## Next Steps
1. Sửa file `.gitignore` và gỡ `.env` khỏi git tracking.
2. Cập nhật `firestore.rules`.
3. Tìm và diệt lỗi CSS syntax.
