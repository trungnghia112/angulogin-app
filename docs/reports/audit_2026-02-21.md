# Audit Report - 2026-02-21

## Focus: PrimeNG p-dialog & p-select Pattern Audit

## Summary
- 🔴 Critical Issues: 0
- 🟡 Warnings: 5 (all fixed)
- 🟢 Suggestions: 0

## 🟡 Warnings (Đã sửa tất cả)

### 1. p-select trong dialog thiếu `appendTo="body"` → Dropdown bị cắt

**Nguyên nhân gốc:** PrimeNG `p-select` mặc định render dropdown panel **bên trong DOM cha** (dialog). Khi dialog có `overflow: hidden/auto`, dropdown bị cắt (clipped). Khi dropdown mở → content area thay đổi → dialog tự tính lại vị trí center → **gây giật (jitter)**.

**Fix chuẩn:**
```html
<!-- SAI -->
<p-select [options]="..." class="w-full" />

<!-- ĐÚNG -->
<p-select [options]="..." [fluid]="true" appendTo="body" />
```

**Files đã sửa:**

| File | Số p-select | Chi tiết |
|------|------------|----------|
| `rpa-task.html` | 3 | template, profile, browser |
| `profile-edit-dialog.html` | 4 | proxy, folder, rotation-mode, rotation-group |
| `home.html` | 1 | mass-proxy-select |

---

### 2. p-dialog thiếu `blockScroll` / `focusOnShow=false` → Giật khi tương tác

**Nguyên nhân:** 
- `blockScroll` ngăn body scrollbar xuất hiện/biến mất khi dialog mở
- `focusOnShow=false` ngăn dialog tự focus/reposition khi mở
- `draggable=false` ngăn vô tình kéo dialog khi click

**Fix chuẩn:**
```html
<p-dialog [modal]="true" [draggable]="false" [blockScroll]="true" [focusOnShow]="false">
```

**Dialogs đã sửa:**

| Dialog | File |
|--------|------|
| Edit Profile | `profile-edit-dialog.html` |
| Create RPA Task | `rpa-task.html` |
| Mass Proxy Change | `home.html` |
| Mass Move to Folder | `home.html` |
| Bulk Create Profiles | `home.html` |

---

## Quy tắc mới (cần tuân thủ)

> **Rule:** Mọi `p-select`, `p-datepicker`, `p-autocomplete`, `p-multiselect` bên trong `p-dialog` PHẢI có `appendTo="body"`.

> **Rule:** Mọi `p-dialog` PHẢI có `[blockScroll]="true"` `[focusOnShow]="false"` `[draggable]="false"`.
